{
  "name": "warframe-name-generator",
  "version": "0.0.0-development",
  "description": "Name Generator for Warframe",
  "main": "index.js",
  "scripts": {
    "test": "npx nyc --reporter=text npx mocha test/ --exit",
    "coverage": "npm test && npx nyc report --reporter=text-lcov | npx coveralls",
    "lint": "npx eslint --ignore-path .gitignore .",
    "lint:fix": "npx eslint --ignore-path .gitignore . --fix"
  },
  "keywords": [
    "warframe",
    "name generator",
    "warframe name generator"
  ],
  "author": "TobiTenno",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=8.9.0",
    "npm": ">=6.1.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^6.1.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "greenkeeper-lockfile": "^1.15.1",
    "mocha": "^5.2.0",
    "sinon": "^6.1.5"
  }
}
